---
description: After graph layer edits, run chat-protocol compliance check vs docs/chat-protocol.md
globs: apps/server/src/graph/**
alwaysApply: false
---

# Graph chat protocol gate

When you change any file under `apps/server/src/graph/`, **before finishing the task**:

```bash
python3 .cursor/skills/graph-chat-protocol-skill/scripts/run_pipeline.py --skip-tsc
```

If checks fail, fix code or update `docs/chat-protocol.md` + checker rules together.

Full workflow: invoke `/graph-chat-protocol-skill` or read `.cursor/skills/graph-chat-protocol-skill/SKILL.md`.
